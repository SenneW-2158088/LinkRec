services:
  fuseki:
    image: stain/jena-fuseki
    ports:
      - "3030:3030"
    environment:
      - ADMIN_PASSWORD=admin
    volumes:
      - ./data:/fuseki/databases
      - ./config:/fuseki/configuration
      - ./files:/fuseki/files
